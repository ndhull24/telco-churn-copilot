{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Install deps",
      "type": "shell",
      "command": "${config:python.defaultInterpreterPath} -m pip install -r requirements.txt"
    },
    {
      "label": "Run server",
      "type": "shell",
      "command": "${config:python.defaultInterpreterPath} -m uvicorn app.api:app --reload --port 8000",
      "problemMatcher": []
    }
  ]
}
